<!DOCTYPE html>
<html lang="ja">
<head><meta charset="UTF-8"><title>Todos</title></head>
<body>
<h1>Todo一覧</h1>

<table border="1">
<tr><th>優先順位</th><th>タイトル</th><th>詳細</th><th>操作</th></tr>

<% data
   .sort((a,b)=>a.priority - b.priority)  // ★優先順位の低い数字から表示
   .forEach(t => { %>
<tr>
  <td><%= t.priority %></td>
  <td><a href="/todos/detail/<%= t.id %>"><%= t.title %></a></td>
  <td><%= t.detail %></td>
  <td>
    <a href="/todos/edit/<%= t.id %>">編集</a> /
    <a href="/todos/delete/<%= t.id %>">削除</a>
  </td>
</tr>
<% }) %>
</table>
<br>

<a href="/todos/create">＋ Todo追加</a>

</body>
</html>
